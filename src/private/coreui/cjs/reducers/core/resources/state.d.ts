import { ActionProps } from '.';
declare const _default: (previousState: {
    fetching: boolean;
    success: boolean;
    failure: boolean;
} | undefined, { type }: ActionProps) => {
    fetching: boolean;
    success: boolean;
    failure: boolean;
};
export default _default;

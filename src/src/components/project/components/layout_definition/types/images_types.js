import { NAMESPACE_MODULE } from '../constant';
const IMAGES_FUNCION = `${NAMESPACE_MODULE}_IMAGES`;
export const RESET = `${IMAGES_FUNCION}_RESET`;
export const ADD = `${IMAGES_FUNCION}_ADD`;
export const DELETE = `${IMAGES_FUNCION}_DELETE`;
export const SELECT = `${IMAGES_FUNCION}_SELECT`;
export default {
    RESET,
    ADD,
    DELETE,
    SELECT,
}